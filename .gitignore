# Dependências (O mais pesado)
/node_modules
/.pnp
.pnp.js

# Builds e Cache (Gerado automaticamente)
/.next/
/out/
/build
/.cache

# Banco de Dados Local (Não enviar)
/prisma/migrations/migration_lock.toml
*.db
*.sqlite
postgres_data/
docker-compose.yml 

# Variáveis de Ambiente (SEGURANÇA: NUNCA ENVIE ISSO)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Arquivos de Sistema
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*